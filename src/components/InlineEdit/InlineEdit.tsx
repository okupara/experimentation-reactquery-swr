import * as React from "react"
import { Box } from "@chakra-ui/layout"

export const InlineEdit: React.FC = (props) => <Box>{props.children}</Box>

export const InlineEditHoverArea: React.FC = (props) => <Box>{props.children}</Box>

export const InlineEditUneditable: React.FC = (props) => <Box>{props.children}</Box>

export const InlineEditEditable: React.FC = (props) => <Box>{props.children}</Box>

export const InlineEditHovering: React.FC = (props) => <Box>{props.children}</Box>
